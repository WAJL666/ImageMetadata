using ImageMetadataTools.UI;

MenuPrincipal menu = new();
menu.Inicio();                Console.WriteLine("❌ El archivo no es una imagen válida.");
                Console.ReadKey();
                return false;
            }
            return true;
        }
    }
}
